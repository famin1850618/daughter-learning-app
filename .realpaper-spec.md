# 真题处理工作流规范（V3.10）

**作用：** 这是真题（.doc/.docx/.pdf → batch JSON）入库的**唯一权威指引**。每会话开始前必读。

**与 .cron-spec.md / .cambridge-spec.md 的关系：**
- `.cron-spec.md`：已停用（V3.8.3）。Anthropic Cloud cron 远程出题规范，历史参考。
- `.cambridge-spec.md`：V3.9 Cambridge 英语题包出题规范（PET/FCE/CAE）。本规范不影响。
- `.realpaper-spec.md`（本文件）：V3.10 数学/语文/物理/化学真题入库规范。

---

## 1. 范围

| 学科 | 处理 | 备注 |
|---|---|---|
| 数学（北师大）| ✅ | 全程 |
| 语文（人教）| ✅ | 全程 |
| 物理（人教）| ✅ | 八九年级 |
| 化学（人教）| ✅ | 九年级 |
| 英语 | ❌ | 走 .cambridge-spec.md（独立体系）|
| AI / 政治 / 历史 / 地理 | ❌ | 不在产品范围 |

**真题来源：** `~/AI_Workspace/真题/{六下,初一,初二,初三,中考}/`（非语数理化目录已清理）

---

## 1.6 已知乱码源跳过列表（V3.12.7 起，agent 入库前必读）

下列 PDF 目录已确认嵌入私有字体（pdftotext 抽出 cid 编码乱码）。入库 agent **整目录跳过，不再尝试**（避免浪费 token）。

| 路径 | 备注 |
|---|---|
| `~/AI_Workspace/真题/六下/语文/6年级下册语文部编版/单元试卷/` | 17 卷全部私有字体（V3.12.7 第三轮 agent 实测）|
| `~/AI_Workspace/真题/六下/数学/6年级下册数学北师大版/期中试卷/`（卷 3/4/5）| 扫描型 PDFPatcher（V3.12.6 第二轮 agent 实测）|
| `~/AI_Workspace/真题/六下/数学/教材帮期末真题卷*.pdf` | 扫描型 PDFPatcher（V3.12.5 第一轮实测）|
| `~/AI_Workspace/真题/六下/数学/小升初数学专题复习/` | 16 个文件全部扫描型（V3.12.5 实测）|

新发现乱码源时 → 加入本表 + 标 manifest.skipped_files，下批 agent 自动跳。

升级 OCR 引擎或得到文字版替代源前，**禁止反复尝试本表内文件**。

---

## 4.3 Source 命名缩写表（V3.12.7 固定，避免 agent 漂移）

`source = realpaper_g{N}_<subject>_<textbook>_<paper_type>_<seq>` 中 `<paper_type>` 必须用以下固定缩写：

### 数学 / 语文 / 物理 / 化学

| 缩写 | 中文 |
|---|---|
| `d{N}_gg` | 第 N 单元过关检测卷 |
| `d{N}_gz` | 第 N 单元跟踪检测卷 |
| `d{N}_kp1` | 第 N 单元（按知识点1组）|
| `zhouce_peiyou_{NN}` | 周测培优卷 NN |
| `qz_{NN}` 或 `qizhong_{NN}` | 期中卷 NN（统一用 qizhong）|
| `qm_{NN}` 或 `qimo_{NN}` | 期末卷 NN（统一用 qimo）|
| `qimo_quanzhen_{NN}` | 期末全真卷 NN |
| `qimo_moni_{NN}` | 期末模拟卷 NN |
| `qimo_mokuai_{NN}` | 期末模拟卷 NN（部编旧名变体）|
| `xsc_{region}_{NN}` | 小升初+地区拼音首字母（**不要用 xshchu**）|
| `kaodian_guoguan_{NN}` | 考点过关卷 NN |
| `kaodian_zonghe_{NN}` | 考点综合卷 NN |
| `mokuai_{topic}_{NN}` | 模块卷 NN（topic: jisuan/yingyong/kongjian/tongji 等）|
| `zhuanxiang_{topic}_{NN}` | 专项卷 NN |

### 反例（V3.12.6 / V3.12.7 实测漂移）

```
❌ realpaper_g6_math_beishida_xshchu_beijing_001  （V3.12.6 用了 xshchu，太长）
✅ realpaper_g6_math_beishida_xsc_beijing_001     （V3.12.7 改 xsc，3 字母）

❌ realpaper_g6_chinese_bubian_qizhong_002 (V3.12.5 入库)
✅ realpaper_g6_chinese_bubian_qizhong_002 ← 一致

❌ realpaper_g6_chinese_bubian_期中_002  （含中文不允许）
```

新增 paper_type 时主 session 先查本表，无对应再加新 entry（commit 加注）。

---

## 1.4 Issues & Resolutions 反馈机制（V3.12.7 强制）

**每轮入库 agent 完成后必须附 "Issues & Resolutions" 报告**，主 session 整理交 Famin 决策是否写入纪律。

**目的：** 从一线 agent 的纠结点识别新规则，让 spec/skill 体系不断细化（observation loop O5/O6 实施）。

**报告格式：**

```
## Issues & Resolutions

### 问题 N: <简述>
- **遇到场景**: 第 X 卷处理时 / 第 Y 题判定时
- **现象**: 具体描述（含原题片段 + agent 当时尝试）
- **当时决策**: 我选了 X，依据 spec §A.B / 自由判断
- **是否合规**: ✅符合 / ⚠ 边界 / ❌违反但权宜
- **建议是否写入纪律**: 是/否 + 理由
```

**报告要求：**
- 全卷处理中**每一个判断犹豫的点**都列出
- 包括但不限于：题型识别歧义 / KP 不在清单边界 / 形式 A vs B 边界 / 图截图选择 / round 判定犹豫 / 加点字识别失败 / 拼音是否带声调判断
- "全顺利没问题"= 思考不深入的**失败信号**
- 所有"我判断了但不确定对不对"的点全暴露

**主 session 处理流程：**
1. 收 agent Issues 清单
2. 整理去重 + 按类型分组
3. 交 Famin 决策（每条标"建议写入纪律 / 个案处理 / 不需要"）
4. Famin 同意写入的 → 回填对应 spec 节 + skill rules/

**触发条件：** 每轮 agent 完成强制执行，不允许跳过。

---

## 1.5 原题保真原则（V3.12.7 顶级硬约束，违反整题撤回）

> **入库 = 原题 1:1 转 batch JSON。除 OCR 矫正错字 + LaTeX 公式化外，禁止任何对原题内容的加工。**

### 严禁的 Agent 加工行为

| 加工行为 | 例子 | 危害 |
|---|---|---|
| ❌ **加释义/翻译/解释** | `ji mo（孤单冷清）` 加括号释义 | **直接剧透答案**（违反 quality_rules §1） |
| ❌ **去除原题标记** | 拼音 `jī mò` → `ji mo`（去声调） | 学生本来要看声调辨析，去掉降低难度 |
| ❌ **添加格式提示** | `（不带声调）` `（填两个词）`（如原题没说） | 改变原题难度 |
| ❌ **改写题面措辞** | `读拼音写词语` → `短文中拼音写字词` | 不是原题文字 |
| ❌ **拆同组合题为独立 question 不用 group_id** | 一段短文 N 个空切 N 道独立题 | 破坏原题语境，UI 不能整组连续做（违反 §5.4） |
| ❌ **题面包含答案** | 拼音后括号写词、"图中三角形（直角三角形）" | 剧透（违反 quality_rules §1 模式 1/3） |
| ❌ **简化原题语料** | 阅读理解原文截断 | 改变题难度（违反 §9.1 完整性优先） |

### 允许的修改（仅这 5 种）

1. **OCR 错字矫正**：明显的 OCR 错字（如"杀"误识为"杂"）按上下文矫正
2. **LaTeX 公式化**：纯文本数学表达 → `$\frac{1}{3}\pi r^{2}$`（§5.7）
3. **加点字 markdown 加粗**：`粽` (加点) → `**粽**`（§5.6）
4. **JSON 字符串转义**：双引号 `"..."` → `「...」`（§5.5）
5. **图嵌入**：`如图` 题加 `image_data` 截图（§9.2）

**除此之外都不允许**。原题怎么写就怎么入库。

### 4a annotate 阶段自检（pre_commit_check.md 第 13 项）

```python
def check_no_agent_doctoring(content: str, original_raw: str) -> list:
    """对比 batch content 跟 PDF 原文 raw.txt，找加工痕迹"""
    issues = []
    # 1. 拼音去声调（jī mò → ji mo）
    if has_pinyin_no_tone(content) and has_pinyin_with_tone(original_raw):
        issues.append('拼音去声调（应保留 jī mò 等声调字符）')
    # 2. 题面添加 () 解释
    # 找 content 里的括号注释，原文不存在
    if has_added_parenthetical_gloss(content, original_raw):
        issues.append('括号添加释义/翻译/解释（剧透答案）')
    # 3. 改写题面措辞
    if title_phrase_modified(content, original_raw):
        issues.append('改写原题措辞')
    return issues
```

发现违反 → 整题撤回，重新按原题写。

### 反例（V3.12.6 第二次重扫实测教训）

```
原题: "短文中读拼音写词语：jī mò_____。"
agent 入库: "短文中拼音写字词：ji mo（不带声调，孤单冷清）"
违反: 1 改写措辞 + 2 去声调 + 3 加格式提示 + 4 加释义剧透
```

V3.12.7 起这是**顶级原则**。所有 agent prompt 必须显式包含本节内容。

---

## 2. 9 步 Pipeline（V3.12.5 起 round 判定与 annotate 分离）

```
真题文件（.doc/.docx/.pdf）
  └→ [1 extract]    libreoffice headless --convert-to txt → .cache/realpaper/<sha1>/raw.txt
  └→ [2 segment]    正则切题（"^一、""1.""(1)"等模式 + 大题套小题用 group_id）→ stems[]
  └→ [3 match-ans]  找"参考答案/答案"段；或独立答案文件配对
  └→ [4a annotate]  Claude 会话级标注：type / chapter (∈158) / KP (∈491) / answer / explanation
                    ⚠ round 字段强制留 null，不许 LLM 自由判
  └→ [4b round]     按科目当前阶段判 round（见 §2.1 三阶段表）：
                      - 阶段 0（无锚点）：LLM 自由推理判
                      - 阶段 1（有锚点+rubric）：LLM 必须读 anchors_*.json + rubric_*.md 判
                      - 阶段 2（skill/脚本成熟）：强制脚本（如数学 d2_math_review_v4.py）
  └→ [5 validate]   py 脚本校验 chapter/KP/题型一致性 + KP 不在清单的题入等候区
  └→ [6 emit]       batch JSON 双写 assets/data/batches/ + question_bank/
  └→ [7 register]   question_bank/index.json + main.dart _bundledBatchAssets
  └→ [8 commit]     git commit + push + 更新 manifest
```

每步详细见 §10 实施细则。

### 2.1 Round 判定三阶段（V3.12.5 起硬规则）

每科 round 判定按"渐进沉淀"演化，**绝不退化**（每次 Famin verify 反馈后必须立即沉淀，让下批扫描受益）。

#### 三阶段定义

| 阶段 | 触发条件 | round 判定方式 | Famin 角色 |
|---|---|---|---|
| **0 无锚点** | 该科目首次扫描 / 锚点尚未建立 | LLM 自由推理判 | 抽审 verify markdown（锚点候选 + 改档）|
| **1 锚点+rubric** | 首轮 verify 反馈完成 → 锚点 + rubric 文档已沉淀 | **LLM 必须读 `anchors_*.json` + `rubric_*.md` 判**（不许凭空判，必须引用锚点 ID）| 继续抽审，反馈累积 |
| **2 skill/脚本** | rubric 稳定 + 累计 ≥ 3-4 轮 verify + 75% 严格对齐 | 脚本强制（python tools/d2_*_review_v*.py）| 仅 flag_review（high_variance / anchor_disagree）抽审 |

#### 各科目当前阶段（2026-05-08）

| 科目 | 阶段 | 判定方式 | 文档资产 |
|---|---|---|---|
| **数学** | ✅ 阶段 2 | `tools/d2_math_review_v4.py` 强制 | `~/.claude/skills/difficulty-math/` (anchors+evaluate+rubric+scripts) |
| **语文** | ⏳ 阶段 0→1（首轮 verify 进行中）| 等 Famin 审完 → 升阶段 1 | `docs/anchor_questions_g6_chinese.json`（20 道未审）+ rubric 待 Famin 审完后沉淀 |
| **英语** | ✅ 阶段 2（特殊路径）| Cambridge composition skill 自带 round（A2/B1/B2/C1 → R1/R2/R3/R4）| `~/.claude/skills/composition-english/`（待建）|
| **物理** | ⏳ 阶段 0 | LLM 自由判 | 待首轮扫描后建立锚点 |
| **化学** | ⏳ 阶段 0 | LLM 自由判 | 待首轮扫描后建立锚点 |

#### 阶段升级触发流程（强制）

每次 Famin verify 反馈后**立即**执行：

```
verify markdown 收到反馈
  ↓
1. 锚点更新：把同意的锚点写入 docs/anchor_questions_<grade>_<subject>.json
              + _famin_review.comment 字段
  ↓
2. Rubric 沉淀：把 reasoning 抽提到 docs/rubric_<subject>.md（首次→新建）
              重点记录"什么档对应什么特征" + "Famin 推翻 LLM 的典型 case"
  ↓
3. 反向修订：revert 项 → 改 batch round 字段（双写）+ commit
  ↓
4. 阶段判定：
     - 首轮 verify 完成 → 阶段 0 → 阶段 1（下批扫描 prompt 必须引用锚点+rubric）
     - 累计 ≥ 3-4 轮 verify + 75% 严格对齐 → 阶段 1 → 阶段 2（生成 reviewer 脚本 + skill）
  ↓
5. 工作流文件同步：
     - .realpaper-spec.md §2.1 各科阶段表更新
     - feedback_round_judge.md 同步
     - 新建 skill 时把锚点+rubric+脚本打包到 ~/.claude/skills/difficulty-<subject>/
```

**审了不沉淀 = 白审**。这是 feedback_strategy_to_execution 的硬要求。

#### 禁止行为

- ❌ 入库 agent 在 4a 阶段直接给 round 字段赋值（哪怕"为方便 4b 跑"）
- ❌ 数学科 LLM 自由判 round（已阶段 2，必须跑脚本）
- ❌ 已进入阶段 1 的科目，agent prompt 不引用锚点+rubric 凭空 LLM 判（这是退化）
- ❌ 跳过 verify 直接把 LLM 判的 round 落库（V3.12.5 commit 161072d 反面教材）
- ❌ Famin verify 完不沉淀就启动下批扫描（必须先沉淀再扫）

---

## 3. chapter 命名规则

### 3.1 数学/物理/化学：教材章节（保持现状）
严格映射 `lib/database/curriculum_seed.dart` 现有数学 40 / 物理 22 / 化学 11 chapter（按北师大/人教教材单元编排）。

### 3.2 语文：按 KP 一级 category
chapter = KP fullPath 的第一段（13 类）：`古诗文` / `现代文阅读` / `写作` / `字词` / `文学常识` / `名著阅读` / `修辞` / `综合性学习` / `句式与标点` / `语法` / `语法与句式` / `课文与名著` / `阅读理解`。

### 3.3 综合卷（小升初/中考）

| chapter | 适用 |
|---|---|
| `小升初综合` | grade=6 数学/语文 真题（覆盖小学全程内容）|
| `中考综合` | grade=9 数学/语文/物理/化学 真题（覆盖初一到初三）|

V3.10 已扩 6 个综合 chapter（curriculum_seed.dart）。

### 3.4 一卷归类策略

- **小升初真题** → grade=6, chapter="小升初综合", KP=题主考内容（可跨小学全年级）
- **中考真题** → grade=9, chapter="中考综合", KP=题主考内容（可跨初一到初三）
- **单元/月考/期末真题** → grade=对应年级, chapter=对应年级 curriculum_seed 章节, KP=题主考内容

文件名信息（地区/年份/类型）写到 `source` 字段，**不进 chapter**。

---

## 4. source 命名规则（每卷唯一）

```
realpaper_g{6-9}_<subject_pinyin>_<region_pinyin>_<paper_type>_<seq>
```

例：
- `realpaper_g6_math_beijing_pinggu_qimo_001`
- `realpaper_g9_chem_2018_zhongkao_shanghai_001`
- `realpaper_g6_chinese_xiaoshengchu_moni_school1_001`

`<seq>` 防同地区同类型多卷冲突。

### 4.1 主观题保留区
常规 source 基础上加 `_subj_held` 后缀：
- 例：`realpaper_g6_chinese_beijing_qimo_001_subj_held`
- QuestionDao 抽题 SQL 已加 `source NOT LIKE '%_subj_held'` 过滤
- 等接入 AI 评分 API 后批量去后缀启用

### 4.2 翻译为英文的中文题（V3.11+ 启用）
原中文题 source 加 `_translated_en` 后缀（暂不抽到，避免重复练）。

---

## 5. 题型识别规则

| 模式 | 判定 type |
|---|---|
| 题面含 4 行 `A./B./C./D.` | choice |
| 题面是单一陈述句 + `(    )` 或 `判断对错`（"对的画√错的画×"等指引）| **judgment**（V3.10 新）|
| 题面含 `_____` 或 `（  ）`且无 ABCD 选项；答案 ≤ 8 字 | fill |
| 题面含 `计算下列` `解方程` `列式` `求...的值` | calculation |
| 题面含 `作文` `根据材料写` `谈谈你的看法` `写一篇` `不少于 X 字` `谈谈感受` | **subjective**（保留区，source 加 `_subj_held`）|

### 5.1 输入法限制白名单（feedback_question_quality.md 继承）
fill 答案必须满足：
- 中文：单字 / ≤4 字短词 / ≤5 字古诗原文 / 修辞名词 / 作家作品名
- 数字：纯数字 / 简单分数（"1/3"）/ 简单比例（"3:1"）
- 不允许：长句 / 整段表达 / 含特殊符号（π/²/×/拼音声调等 → 改 choice）

### 5.2 主观题归类
- 语文作文/简答/论述 → subjective + `_subj_held`（家长批改机制 V3.8.3 已建，但暂不启用，等 AI API）
- 政治/历史的简答（不在范围）→ 跳过整题
- 数学/物理/化学的简答（如"简述实验步骤"）→ subjective + `_subj_held`

### 5.3 判断题（V3.10 新）
- type=`judgment`；answer 必须是 `"对"` 或 `"错"`（单字）
- AnswerMatcher 接受等价写法：对/正确/√/T/true/yes 视为"对"；错/错误/×/F/false/no 视为"错"
- options 字段填 `null`（UI 自动渲染对/错二选一按钮）
- alt_answers 不需要（matcher 已内置归一化）

### 5.4 系列组合题纪律（V3.12.5 全局原则，适用所有科目）

**总原则（绝对禁令）：**

> **绝不把一道大题拆成多道完全独立的题。** 必须保留共同题面，用 `group_id` 关联形成系列组合题。

适用所有科目（数学/语文/英语/物理/化学），以及所有有"共同上文"的复合结构：
- 大题指令 + 多个并列子问题（如"在下列句子中填关联词：(1)...(2)...(3)..."）
- 候选池 + 多个语境填空（如"从下列成语中选填：成语 A/B/C... (1)...(2)..."）
- 阅读材料 + 多个理解小题（如"阅读下文后回答：(1)...(2)...(3)..."）
- 图/表/材料 + 多个分析小题
- 数学应用题"已知条件 + 多问"（如"(1) 求...; (2) 在 (1) 的基础上...; (3) 拓展..."）
- 英语 cloze test、reading comprehension 多 question
- 物理化学的实验题、综合题

**两种处理形式（按子题"形式统一性 + 长度"判断）：**

### 形式 A：压成 1 道多空 fill（V3.12.7 优先选）

**适用条件（必须全部满足）：**
- 所有子题**同一题型**（全 fill 或全 choice）
- 所有子题**同一题面指令**（如全部"看拼音写词语"，全部"填关联词"，全部"古诗背诵默写"）
- 子题文本短，答案是离散单字/短词（≤4 字）
- 答案之间无依赖关系（不是"题(1) 求 X，题(2) 用 X 求 Y"）

**入库格式：**
```yaml
type: fill
content: "[共同题面 + 列出所有空]"
answer: "答案1,答案2,答案3,..."  # 用逗号分隔，按原题顺序
```

**举例：**
```yaml
原题: "读拼音写词语。chú xī(___) chūn lián(___) rán fàng(___) biān pào(___)..."

入库 (1 道 fill):
  type: fill
  content: "读拼音写词语。chú xī(   ) chūn lián(   ) rán fàng(   ) biān pào(   )..."
  answer: "除夕,春联,燃放,鞭炮,..."
```

UI 学生输入"除夕,春联,燃放,..."一次答完整题。AnswerMatcher 已支持逗号分隔判错（V3.7 起）。

### 形式 B：N 道独立 question + group_id（V3.12.5 沿用）

**适用条件（任一满足即用）：**
- 子题**题型混合**（fill + choice 混合，如"先补全词语再选词填空"）
- 子题**长度长**（如阅读理解每子题独立段落 + 长答案）
- 子题**答案有依赖**（题(2) 基于题(1) 答案）
- 含**复杂候选池**（候选池 ≥ 子题数，需独立 choice）
- 含**阅读理解 / 长材料**（每子题保留完整原文）

**统一字段：**

| 字段 | 含义 |
|---|---|
| `group_id` | 同一大题所有子题共享（如 `"qizhong_002_q15"`）|
| `group_order` | 子题顺序（1, 2, 3, ...）|
| `content` | **必须含共同题面**（重复也保留），后接当前子题文字 |
| `type` | 子题类型独立判（fill / choice / judgment 可混合）|

举例（阅读理解保留完整原文）：
```yaml
所有子题 group_id="qizhong_002_q15"，type=choice：

子题 1 (group_order=1):
  content: "阅读《人类能在地球上生活多久》：[完整 700 字说明文]\n问：'来自人类自身'的威胁主要指什么？"
  options: [...]
  answer: "D"

子题 2 (group_order=2):
  content: "阅读《人类能在地球上生活多久》：[完整 700 字说明文]\n问：'这一过程'指什么？"
  options: [...]
  answer: "B"
```

### 选 A 还是选 B 的判断流程

```
所有子题同一题型且题型为 fill?
├─ 否 → 选 B
└─ 是 → 所有子题同一题面指令?
        ├─ 否 → 选 B
        └─ 是 → 所有答案 ≤4 字 + 无依赖?
                ├─ 否 → 选 B
                └─ 是 → 选 A（压 1 题多空 fill）
```

### 禁止做法

- ❌ 把子题切成完全独立的 question 不用 group_id（V3.12.5 d1_kp1 #13-16 / #17-20 反面教材：候选池/上文丢失）
- ❌ 用不同 group_id 把同一大题分组（如 _a / _b 子组）—— 同一大题必须同一 group_id
- ❌ 形式统一的拼音填空 / 古诗默写却拆成 N 道独立 question（V3.12.6 反面教材：UI 重复显示 N 次相同题面）—— 应用形式 A 压 1 题
- ❌ 形式 A 压题时 content 没列出所有空 / answer 没用逗号分隔多答案

**正确做法（举例）：**

部编 d1 单元卷 1 第 5 大题"在下列句子中填入恰当的关联词语：(1)过新年... (2)这___粽子，___小型的农业展览会... (3)今日腊八粥... (4)春节___很快乐..."

```yaml
共同题面: "在下列句子中填入恰当的关联词语。"
4 道子题，同一 group_id="d1_kp1_q5"，group_order=1..4：

子题 1 (group_order=1):
  type: fill
  content: "在下列句子中填入恰当的关联词语：过新年，小孩子(  )可以买到各种各样的玩意儿，(  )可以放鞭炮。请按顺序填两个关联词，用逗号分隔。"
  answer: "不但,而且"

子题 2 (group_order=2):
  type: fill
  content: "在下列句子中填入恰当的关联词语：这(  )粽子，(  )小型的农业展览会。请按顺序填两个关联词，用逗号分隔。"
  answer: "不是,而是"

# ... 子题 3 / 子题 4 同理
```

content 共同题面（"在下列句子中填入恰当的关联词语"）每道都重复一次——这是必须的，让题独立可读。

**UI 渲染（V3.8.2 已实现）：** 同 group_id 的题在练习/测评中按 group_order 连续呈现，错题集按"大题"聚合复盘。

**抽题策略：** 同 group_id 题群整体抽（不打散），保持原题的"系列做"体验。

**实施位置：** Pipeline §2 第 2 步 `segment.py` 必须识别"大题→子题"层级，切题时维持 group_id + group_order；4a annotate 阶段每道子题 content 必须含共同题面。无法判定大题边界时整道入"等候区"等人工审核。

### 5.7 LaTeX 公式化纪律（V3.12.7 全科强制，主要影响数学/物理/化学）

**总原则：** 数学题里**所有数学表达必须用 `$...$` LaTeX 公式化**，禁止纯文本写分数/比例/上下标/特殊符号。

**强制公式化的写法：**

| 纯文本（错误）| LaTeX（正确）| 渲染 |
|---|---|---|
| `1/3` | `$\frac{1}{3}$` | 分数 |
| `1/3πr²h` | `$\frac{1}{3}\pi r^{2} h$` | 体积公式 |
| `2:3` 比例 | `$2:3$` 或保留 `2:3` 在 `$...$` 内 | 比例 |
| `r²` | `$r^{2}$` | 上标 |
| `²` `³` `½` | `$^{2}$` / `$^{3}$` / `$\frac{1}{2}$` | 各种符号 |
| `π` 单字 | `$\pi$` 或 content 含 π 字符（V3.7.9 渲染保留 π）| 圆周率 |
| `×` `÷` | `$\times$` / `$\div$`（在公式内）| 乘除 |
| `≠` `≤` `≥` | `$\neq$` / `$\leq$` / `$\geq$` | 不等号 |

**MathText widget 仅识别 `$...$` 单美元符**，禁止：

- ❌ `$$...$$`（双美元符是 LaTeX 块级公式语法，不支持）
- ❌ `\(...\)` 或 `\[...\]`（不支持）
- ❌ 纯文本含分数线/上标/比例（V3.12.6 数学 5 处明文分数 + 1 处比例反面教材）

**4a annotate 阶段必检：**

```python
import re
def has_unformatted_math(text: str) -> list:
    """扫 content/explanation 找未公式化的数学表达"""
    issues = []
    # 把 $...$ 内的内容剔除后检查
    stripped = re.sub(r'\$[^$]+\$', '', text)
    # 明文分数 a/b（数字间）
    if re.search(r'(?<!\d)\d+/\d+(?!\d)', stripped):
        issues.append('明文分数')
    # 明文上标 ²³
    if re.search(r'[²³⁴⁵]', stripped):
        issues.append('明文上下标')
    # 含 r² h³ 等不在公式内的字母+上标
    return issues
```

入库前 self-check 调用此函数，发现违反**整题撤回**或自动修正。

**反例（V3.12.6 实测）：**

```
realpaper_g6_math_beishida_d1_genzong_001#9 explanation:
  "帐篷容纳空间＝圆锥体积＝(1/3)πr²h..."
  ↑ 应改为 "帐篷容纳空间＝圆锥体积＝$\frac{1}{3}\pi r^{2} h$..."

realpaper_g6_math_beishida_d1_genzong_001#10 explanation:
  "圆柱−圆锥＝圆柱的 2/3＝42..."
  ↑ 应改为 "圆柱−圆锥＝圆柱的 $\frac{2}{3}$＝42..."
```

V3.7.9 已立 LaTeX 纪律但每次入库 agent 都漏。V3.12.7 起作为**入库前自检 11 项之一**强制。

---

### 5.6 加点字 / 强调字：文本标记替代图形（V3.12.7 全科适用）

#### 5.6.1 已知加点字格式（两类，V3.12.7 实证）

| 类型 | 标记字符 | PDF 示例 | pdfplumber 检测支持 |
|---|---|---|---|
| **A 标准加点** | `．` (U+FF0E) 在 char bbox 内底部 | "杂**拌** **绊**倒"（拌底有 ．）| ✅ extract_emphasis.py 直接检测 |
| **B 多音字辨析** | 文字加点 + `(候选 1 候选 2)` 紧跟 | "畜(xù chù)牧业... 牲畜(xù chù)" | ❌ pdfplumber 不识别 |

#### 5.6.2 Agent 入库强制流程（V3.12.7 起）

每张 PDF 必须**两步检测**：

```bash
# Step 1: 跑 pdfplumber 检测（A 标准加点）
python tools/realpaper/extract_emphasis.py <pdf> > /tmp/emphasis_a.json

# Step 2: 凭 PDF 渲染图（pdftoppm 200 dpi 预览）肉眼扫 B 多音字辨析格式
# 触发标志: 题面含"加点字" + 文字后括号有候选读音/释义
# 这种"加点字+(候选)"是 B 类，pdfplumber 漏检测，agent 必须凭文本补
```

**Agent 必须做的核对：**
1. 拿 pdfplumber 输出（A 类加点）
2. grep 全 raw.txt 找题面含"加点"字样的题
3. 每道"加点"题对照 PDF 渲染图肉眼检查—— A 类 / B 类 / 两者都有
4. **不允许漏标**：每个加点字都用 `**字**` 标记
5. **不允许凭空加**：必须有 PDF 原题依据（A: pdfplumber 检测 / B: PDF 渲染图肉眼可见）

#### 5.6.3 反例 + 自检（V3.12.7）

```
qizhong_008 实测（V3.12.7 第三轮）:
  pdfplumber 检测: 22 处加点字
  agent 入库标记: 7 处
  漏标: 15 处 → 违反 §9.1 完整性

修复: agent 必须对照 pdfplumber 输出全标，禁止偷工。
```

入库前自检（pre_commit_check.md 第 14 项 V3.12.7 新增）：
- 跑 pdfplumber 检测对比 batch 加点字标记数
- 漏标 > 0 → 整题撤回（不允许"已经过 70% 准确率就提交"）

#### 5.6.4 工具升级路线图（待实施）

`tools/realpaper/extract_emphasis.py` V0.2 计划：
- 加 B 类格式检测：`raw.txt` 里 `字(候选1 候选2)` 模式 + 题面"加点"提示 → 推断该字加点
- 跟 A 类输出合并

短期 agent 仍按 5.6.2 两步检测，工具升级后改为单步。

---

#### 5.6.5 文本标记 + UI 渲染（V3.7.9 沿用）

**适用：** 中文真题"加点字读音/字义辨析"题型，以及任何字符底部带 · 标注的题。

**原则：** 关键信息（哪个字加点）从图形维度迁移到文本维度，不算丢失信息（§9.1 完整性）。文本可搜索/复制/放大/朗读 TTS，比截图更优。

**强制做法：**

1. **检测**：4a annotate 阶段用 `pdfplumber` 或 `pymupdf` 扫 PDF 字符坐标，识别字符下方 3-8 px 内的 · ・ ･ 等中点字符
2. **标记**：被加点的字用 markdown 加粗 `**字**` 写入 content
3. **注释**：explanation 末尾自动加 "（**加粗字**为原题加点字）"
4. **UI 渲染**：`lib/utils/math_text.dart` MathText widget 已支持 `**字**` 解析（V3.12.7）

**例子：**

原题（PDF 视觉）：
```
下面句子中加点的字读音正确的是（  ）
A. 这个粽子真好吃。 (粽 zòng)
              ·
B. 我们正在过端午节。 (端 duān)
        ·
```

入库 content：
```
下面句子中加点的字读音正确的是（  ）
A. 这个**粽**子真好吃。
B. 我们**正**在过端午节。
```

**禁止做法：**

- ❌ 因 pdfplumber 检测失败而整题跳过 → 应改走截图 §9.2 兜底（极少数情况）
- ❌ 默认走 §9.2 截图 → 加重 batch JSON 体积，丢失文本可搜索特性
- ❌ 不加注释 explanation → 学生看不出加粗为何意

**兜底层级：**

1. 首选：pdfplumber 文本坐标检测 + `**字**` 标记
2. 次选：pymupdf 替代检测（如 pdfplumber 失败）
3. 末选：§9.2 截图嵌入（如两种文本检测都失败）
4. 最末：§9.4 整题跳过（仅当截图也模糊到看不清）

**实施工具：** `tools/realpaper/extract_emphasis.py`（V3.12.7 新建，pdfplumber 实现）

---

### 5.5 复合题：fill + choice 类型混合（§5.4 的子情况）

**适用结构：** 大题先给一组待补全的词/句（A 部分），再用补全后的词去填语境空（B 部分）。典型如部编六下 d1 单元卷 1 第 4 大题：

```
4. 把下列四字词语补充完整，并选词填空。
   万(  )(  )新   (  )(  )不同   万不(  )(  )
   (  )灯(  )彩  (  )(  )分明   两(  )三(  )
   (1) 春节到了，中华大地上到处____，显出一派____的气象。
   (2) 在外边做事的人，除非____，必定赶回家来吃团圆饭。
   (3) 少数民族和汉族的饮食风俗虽然____，但各族人民热爱祖国、热爱民族大团结的心是相同的。
```

**遵循 §5.4 全局原则：所有子题用同一个 `group_id`（不分 _a / _b）**，type 可不同：
- A 部分子题 type=`fill`（补全字形）
- B 部分子题 type=`choice`（候选池 = A 部分补全后的词）

```yaml
group_id: "d1_kp1_q4"  # 整大题共享

A 部分（补全题，group_order=1..6）:
  type: fill
  content: "下列大题第 4 题为补全词语并选词填空。先补全 6 个四字词语，再用补全后的词回答 (1)(2)(3) 三句。当前小题：补全四字词语「万(  )(  )新」"
  answer: "象更"

B 部分（选词填空，group_order=7..9）:
  type: choice
  content: "下列大题第 4 题为补全词语并选词填空。补全后的 6 个成语：万象更新 / 截然不同 / 万不得已 / 张灯结彩 / 黑白分明 / 两面三刀。当前小题：(1) 春节到了，中华大地上到处(    )，显出一派___的气象。本题选第 1 个空。"
  options: ["万象更新", "截然不同", "万不得已", "张灯结彩", "黑白分明", "两面三刀"]
  answer: "张灯结彩"
```

**关键纪律：**
1. **同一 group_id**（不要分 _a / _b 子组，§5.4 已明确）
2. **每个子题 content 必须包含共同题面**（"下列大题第 4 题为补全词语并选词填空..."）
3. A 部分考字面识记 → fill
4. B 部分考候选辨析 → choice + options 显式列出 A 部分答案集

**多语境空的处理：**
- 一句含 N 个空（如"到处___，显出一派___"）→ 拆 N 道 choice，content 标"本题选第 K 个空"
- 候选池可重复使用 → options 列全部 6 个；候选池一次性消耗 → content 注明"每个成语只用一次"

**小变体：**
- A 部分极少（≤2 道）→ 仍拆 type，不要合并
- A 部分 = 课内默写（如"补全古诗：少壮不努力，___"）+ B 部分语境填 → A 仍 fill；B 候选池 = A 答案集

**判定 round：**
- A 部分 fill：字面识记，多数 R1
- B 部分 choice：候选池辨析，一般 R2；候选数 ≤ 3 且语境直白可降 R1

**实施位置：** Pipeline §2 第 2 步 `segment.py` 识别"...补充完整，并选词填空"或"...选择恰当的成语填入下列各句"等大题指令，按本节拆。无法判定时整道入"等候区"等人工审核。

---

## 6. 难度档（round）判定

由 Claude 主观判，基于 3 个维度：

### 6.1 试卷类型基线

| 试卷类型 | 整卷 round 基线 |
|---|---|
| 单元/小测 | R1 |
| 月考 | R1-R2 |
| 期中/期末 | R2-R3 |
| 中考真题 | R2-R4 全 |
| 培优/竞赛 | R3-R4 |

### 6.2 卷内位置加权
- 卷首选择前 1/3：基线 -1（最低 R1）
- 卷面中段：基线
- 卷尾大题/压轴：基线 +1（最高 R4）

### 6.3 KP 复杂度
- 单 KP 题：保持上述判定
- 跨年级综合题：**取最高年级 KP**（强制规则）
  - 7 + 9 年级 KP → 归 9 年级 KP
  - 7 + 8 + 9 → 归 9 年级 KP

### 6.4 round 含义（V3.8 沿用）
- R1：基础（教材内单一 KP 直接套用）
- R2：中等（同章节多 KP 综合）
- R3：较难（跨章节综合 / 应用题情境）
- R4：竞赛/压轴（跨年级综合 / 创新探究 / 拓展深度）

### 6.5 组合题难度统一（V3.12.5 全科强制）

**规则：** 同 `group_id` 的所有子题，最终 round = `ceil(各子题原始 round 平均值)`，统一写回每个子题的 `round` 字段。

**适用：** 所有科目（数学/语文/英语/物理/化学），所有 round 判定阶段（脚本判 / LLM 判 / 自由判）。

**为什么：**
- 系列组合题（§5.4）UI 上按 `group_id` 整组连续呈现给学生，整组应有"统一难度感受"
- 子题难度差异大时（如 R1+R2+R4），整组体验受最难子题主导，向上取整体现这点
- 错题集按"大题"聚合复盘，整组用同一个 round 标签更直观
- 抽题策略按 round 抽，整组同 round 才能整组抽

**算法：**

```python
import math
from collections import defaultdict

def unify_group_round(batch_questions):
    """4b round 阶段末尾必跑：同 group_id 的子题统一 round = ceil(平均)"""
    groups = defaultdict(list)
    for q in batch_questions:
        if q.get("group_id"):
            groups[q["group_id"]].append(q)
        # 单题无 group_id 不处理

    for gid, qs in groups.items():
        if len(qs) <= 1:
            continue  # 单子题（虽然有 group_id 但只 1 道）不处理
        rounds = [q["round"] for q in qs if q.get("round") is not None]
        if not rounds:
            continue
        avg = sum(rounds) / len(rounds)
        unified_round = math.ceil(avg)
        # 写回每个子题
        for q in qs:
            q["_round_before_group_unify"] = q["round"]  # 留痕
            q["round"] = unified_round
```

**执行时机：**
- pipeline 4b round 阶段末尾（写回 batch round 字段之前）
- 在数学 V4 脚本输出后、写回 batch 之前调用
- 在语文 LLM 判 round 后、写回 batch 之前调用
- 在物理/化学自由判后、写回 batch 之前调用

**举例：**

| 子题 round | 平均 | ceil | 统一后 |
|---|---|---|---|
| R1+R2+R3 | 2.0 | 2 | R2 |
| R1+R2+R2 | 1.67 | 2 | R2 |
| R1+R1+R2 | 1.33 | 2 | R2 |
| R2+R3+R4 | 3.0 | 3 | R3 |
| R3+R4 | 3.5 | 4 | R4 |
| R1+R1 | 1.0 | 1 | R1 |
| R2+R2+R2 | 2.0 | 2 | R2 |

**留痕字段：** `_round_before_group_unify` 记原始判定值，便于将来 reviewer 反思"组内 round 差异大是否反映出题逻辑问题"。

**禁止例外：**
- ❌ "这组只有 1 道是 R3，其他都 R1，统一到 R2 太高了" → 仍按 ceil(1+1+1+3)/4 = 2.0 = ceil → R2
- ❌ "用 round 字段区分组内难度差异" → 已有 `_round_before_group_unify` 留痕，对外只暴露统一 round

---

## 7. KP 严格匹配 + 等候区机制

### 7.1 严格匹配（feedback_kp_discipline.md 继承）
题的 `knowledge_point` 字段必须**严格**等于 `lib/database/knowledge_points_seed.dart` 的 fullPath（区分大小写、空格、括号）。

**禁止**自创变种 / 自动 add 新 KP。

### 7.2 等候区机制（V3.10 新）
不在 491 KP 清单的题：
- **不入库**
- 写到 `docs/realpaper_kp_pending.json`
- 含字段：source_file / batch_source / question_data（完整题对象 + KP=null）/ claude_suggestion (`candidate_kp` + `action: new_kp/merge_to/drop` + `merge_target` + `reason`)

### 7.3 每科 10 卷 review
我累计该科 10 卷的 `kp_pending` → 按候选 KP 聚类 → 给 Famin 看：
- 候选 KP 名
- 涉及题数
- 示例题面 2-3 个
- 我的建议（merge_to / new_kp / drop）
- 现有相近 KP（如有）

Famin 三选一：
1. **合并** → 题改 KP 后入 `realpaper_g{N}_{科}_kp_supplement` batch
2. **新建** → 先扩 `knowledge_points_seed.dart`，再入 supplement batch
3. **放弃** → 删等候区记录

---

## 8. 答案匹配

按优先级：

1. **同文件含答案段**（~52% 文件名带"含答案"/"试题及答案"）
   - 在 raw.txt 里找 `参考答案|答案|Answer Key` 之后段
   - 题号映射（"1." → 答案 "1." 后内容）

2. **独立答案文件**（同目录下 `*答案*` `*解析*` 同名文件）
   - 配对加载 + 题号对齐

3. **完全无答案**
   - Claude 会话级求解
   - 标记 `answer_source: claude_solved` 写入 explanation 末尾

---

## 9. 图片/质量纪律（适用所有科目，Famin 决策）

### 9.1 完整性优先（V3.12.5 强化）：要么完整入库，要么整题放弃

**原则：** 一道题入库时**必须包含原题中的所有信息**——题面、候选池、共同上文、阅读材料、附图、附表、附说明等一个不漏。**任何一项识别不清或残缺，整题跳过**，不要"部分入库 + 缺什么就丢什么"。

**触发整题跳过的条件（任一满足即放弃）：**
- OCR 失败 / 扫描质量差 / 关键文字模糊
- 嵌入私有字体导致 pdftotext 出乱码
- 附图/几何图模糊看不清
- 听力音频质量差
- 阅读题原文缺失或被切断
- 候选池/共同上文残缺（参见 §5.4 系列组合题纪律）
- 任何"靠猜"才能补上的信息

**禁止做法：**
- ❌ "这道题文字看清了，图模糊就丢图入库" → 整题跳过
- ❌ "候选池没扫到，按语境硬填一个答案入库" → 整题跳过
- ❌ "答案配对模糊，但题目内容还能用，先入库再说" → 整题跳过
- ❌ "OCR 漏几个字，多模态/重绘补上" → 整题跳过
- ❌ "标 _unverified 等以后修" → 直接删除/不入库

**记录：** 跳过的题/卷写到 `manifest.skipped[]`，注明原因。**不要**浪费 token 抢救。

**理由：** 真题量足够（150+ 卷），找模糊题修复成本远高于换下一卷。残缺入库会污染题库（Famin V3.10 多模态幻觉伪题 122 道事件已是反面教材）。

详见 `feedback_unclear_abandon.md`（同一原则的记忆侧）。

### 9.2 清晰图片 → 必须截图嵌入（V3.12.5 强化为强制）

**适用所有科目**（数学/语文/英语/物理/化学）：

> 凡题面提到"如图""下图""右图""示意图""图中""图所示""观察图""看图"等任意指示语，**必须**按 §9.2 / §9.3 之一处理。**禁止**入库时不带图。

**§9.2 截图嵌入路径（首选）：**

```bash
pdftoppm -png -r 200 source.pdf cache/page  # 高分辨率渲染整页
# 用 PIL/imagemagick 裁剪到题对应区域 → PNG → base64
# 写入 question.image_data 字段（V3.7.6 schema 支持）
```

**约束：**
- base64 图 ≤ 200KB（超出则降低分辨率或裁剪更紧）
- 同 batch 多图各自独立 base64，不复用
- 多图题（如 4 选 1 看 4 张图比大小）→ 4 张图都嵌入 image_data（数组）

### 9.3 需要重绘的图 → 重绘（适用所有科目）

适合"直接截图反而模糊或带太多无关元素"的情况：

- 数学几何图 → SVG（V3.7.9 渲染支持）
- 物理电路图 → SVG
- 化学分子结构 → SVG 或 LaTeX
- 化学方程式 → LaTeX `$...$`（V3.7.9 已支持）
- 数学公式 → LaTeX `$...$`

写入 question.content 内嵌 `<svg>...</svg>` 或公式 `$...$`。

### 9.4 何时跳过整题（V3.12.5 严格收紧）

**仅在以下情况跳过整题，记入 `manifest.skipped[]`：**

- ✅ 扫描质量差，图本身模糊到看不清（PDF 模糊 / 嵌入私有字体 / OCR 后字图都丢失）
- ✅ 需要画图作答（学生题目要求"在方格纸上画出..."、"作 XX 的高..."等，无法用 app 输入法回答）
- ✅ 图复杂到截图也无意义（如电路连线图，数据全在线条颜色/位置上，截图后学生看不清）

**禁止理由（V3.12.5 起列入反例）：**

- ❌ "图复杂，重绘 SVG 不值得就跳过" —— 改走 §9.2 截图路径，截图比重绘便宜
- ❌ "agent 不会用 pdftoppm，跳过省事" —— 必须学会用 pdftoppm
- ❌ "title 提到图但题面文字够用，丢图入库" —— V3.12.5 §9.1 完整性原则禁止
- ❌ "条形/扇形/折线统计图复杂，跳过" —— 截图嵌入即可
- ❌ "立体几何图截图也看不清" —— 看具体题，多数 200dpi 渲染清晰可读
- ❌ "审核 token 贵跳过快" —— 缺图入库 = 残缺 = §9.1 违反

**反例（V3.12.5 实测）：**
V3.10 入库 6 卷 math 北师大单元卷，agent 默认走 §9.4 跳过，实际跳掉 5-8 道/卷的图依赖题，但其中 80%+ 是清晰条形图/示意图，本应走 §9.2 截图嵌入。1050 题 0 图就是反面教材。

### 9.5 入库 agent 调用纪律（V3.12.5 强制）

每次入库 agent 处理一卷时：

1. **预扫**：先用 `pdftoppm -png -r 100 file.pdf preview` 把全卷渲染成 PNG，肉眼粗看哪些题带图
2. **分类**：每道题按 §9.2 / §9.3 / §9.4 决策：
   - 清晰图（含表/统计图）→ §9.2 高清截图（200 dpi）+ 裁剪 + image_data 字段
   - 简单几何/坐标 → §9.3 SVG 重绘（agent 直接生成）
   - 真模糊 / 画图作答题 / 极复杂连线 → §9.4 跳过 + manifest.skipped 注明具体原因
3. **入库前自检**：扫 batch JSON 里所有 content 含"如图/下图/右图/示意图/图中/图所示/看图/观察图"等关键词的题，**必须**有 `image_data` 或 content 内嵌 `<svg>`，否则**整题撤回**重处理
4. **commit message** 报告：`图嵌入 N 道（image_data X / SVG Y），§9.4 跳过 Z 道（具体原因列出）`

**禁止**：入库后 batch JSON 里出现"题面提图但 image_data 为 null 且无 SVG"的题。这是硬约束，validate.py 应加检查（TODO）。

---

## 10. 实施细则

### Step 1: extract.py
```python
# 用 libreoffice headless 转纯文本，sha1 缓存
sha1 = hashlib.sha1(open(path, 'rb').read()).hexdigest()
cache_dir = f'.cache/realpaper/{sha1}'
if not os.path.exists(f'{cache_dir}/raw.txt'):
    subprocess.run(['libreoffice', '--headless', '--convert-to', 'txt', '--outdir', cache_dir, path])
return open(f'{cache_dir}/raw.txt', encoding='utf-8').read()
```

### Step 2: segment.py
正则切题：
```
re.split(r'(?:^|\n)\s*(?:[一二三四五六七八九十]+[、.])', text)  # 大题
re.split(r'(?:^|\n)\s*\d+\s*[.、]', text)                    # 小题
re.split(r'(?:^|\n)\s*\(\d+\)', text)                        # 子题
```

### Step 3: match_ans.py
```python
# 找答案段
ans_start = re.search(r'(参考答案|答案|Answer Key|【答案】)', text)
if ans_start:
    ans_text = text[ans_start.end():]
    # 题号映射：1. xxx 2. xxx
    answers = dict(re.findall(r'(\d+)[.\s]+([^\n]+)', ans_text))
```

### Step 4: annotate（Claude inference）
对每题，Claude 输出 JSON：
```json
{
  "stem_idx": 5,
  "type": "choice",
  "chapter": "圆柱与圆锥",
  "knowledge_point": "圆柱与圆锥/圆柱体积",
  "round": 2,
  "explanation": "..."
}
```

### Step 5: validate.py
检查：
- chapter ∈ curriculum_seed（按 subject + grade 过滤）
- knowledge_point ∈ knowledge_points_seed（按 subject 过滤）
- type 一致（题面有 ABCD ↔ type=choice）
- 不在清单的 KP → 转 kp_pending.json

### Step 6: emit batch JSON
```json
{
  "source": "realpaper_g6_math_beijing_pinggu_qimo_001",
  "subject": "math",
  "grade": 6,
  "knowledge_points_added": [],
  "questions": [...]
}
```

双写到：
- `assets/data/batches/<source>.json`
- `question_bank/<source>.json`

### Step 7: register
- `question_bank/index.json` 加 batches[] 项 + version+1
- `lib/main.dart` `_bundledBatchAssets` 追加 `'assets/data/batches/<source>.json'`

### Step 8: commit
- `git add` 上述所有改动
- `git commit -m "Realpaper batch: <region> <grade> <subject> <type>"`
- `git push origin main`
- 更新 `docs/realpaper_manifest.json`

---

## 11. observations 反哺（各科独立）

每处理 5 卷，追加对应 observations 文档：
- `docs/realpaper_observations_math.md`
- `docs/realpaper_observations_chinese.md`
- `docs/realpaper_observations_physics.md`
- `docs/realpaper_observations_chemistry.md`

记录：
- 题面句型套路
- 干扰项设计模式
- 难度梯度规律
- KP 组合习惯
- 答案颗粒度
- 卷型差异

下次会话开始时**必读**对应 observations，提升标注效率与质量。

---

## 12. 失败回滚 / 错误处理

| 错误类型 | 处理 |
|---|---|
| libreoffice 转换失败 | manifest.skipped + 等待手工处理 |
| OCR 扫描质量太差 | 整文件跳过 + manifest.skipped |
| 切题失败（题号识别不出）| 整文件跳过 + manifest.issues |
| 答案缺失 | Claude 求解 + explanation 标 `answer_source: claude_solved` |
| KP 不在清单 | 入 kp_pending.json |
| chapter 不在清单 | 这是 bug，必须修（不允许进等候区）|
| 题型识别歧义 | Claude 二选一 + explanation 注明 |
| 同 sha1 文件已处理 | 跳过整文件（manifest 校验）|
| 单 commit 题量过大（> 500）| 拆批 commit |

---

## 13. 不要做的事

- ❌ 不要改老 cron AI 出的题包（12 个 batch 已 deprecated）
- ❌ 不要使用清单外的 chapter（必须严格映射 curriculum_seed）
- ❌ 不要使用清单外的 KP（不在清单 → 入等候区）
- ❌ 不要自动 add 新 KP（必须 Famin 拍板）
- ❌ 不要把英语真题入库（英语全走 Cambridge AI）
- ❌ 不要为不清晰的图浪费 token 修复
- ❌ 不要把主观题（作文/简答）入正式抽题池（必须 `_subj_held` 后缀）
- ❌ 不要 push 含敏感信息（PAT / API key）

---

**版本：** 2026-05-08 V1（V3.10 立项时由 Famin 与 Claude 共同制定）
