# 真题处理工作流规范（V3.10）

**作用：** 这是真题（.doc/.docx/.pdf → batch JSON）入库的**唯一权威指引**。每会话开始前必读。

**与 .cron-spec.md / .cambridge-spec.md 的关系：**
- `.cron-spec.md`：已停用（V3.8.3）。Anthropic Cloud cron 远程出题规范，历史参考。
- `.cambridge-spec.md`：V3.9 Cambridge 英语题包出题规范（PET/FCE/CAE）。本规范不影响。
- `.realpaper-spec.md`（本文件）：V3.10 数学/语文/物理/化学真题入库规范。

---

## 1. 范围

| 学科 | 处理 | 备注 |
|---|---|---|
| 数学（北师大）| ✅ | 全程 |
| 语文（人教）| ✅ | 全程 |
| 物理（人教）| ✅ | 八九年级 |
| 化学（人教）| ✅ | 九年级 |
| 英语 | ❌ | 走 .cambridge-spec.md（独立体系）|
| AI / 政治 / 历史 / 地理 | ❌ | 不在产品范围 |

**真题来源：** `~/AI_Workspace/真题/{六下,初一,初二,初三,中考}/`（非语数理化目录已清理）

---

## 2. 9 步 Pipeline（V3.12.5 起 round 判定与 annotate 分离）

```
真题文件（.doc/.docx/.pdf）
  └→ [1 extract]    libreoffice headless --convert-to txt → .cache/realpaper/<sha1>/raw.txt
  └→ [2 segment]    正则切题（"^一、""1.""(1)"等模式 + 大题套小题用 group_id）→ stems[]
  └→ [3 match-ans]  找"参考答案/答案"段；或独立答案文件配对
  └→ [4a annotate]  Claude 会话级标注：type / chapter (∈158) / KP (∈491) / answer / explanation
                    ⚠ round 字段强制留 null，不许 LLM 自由判
  └→ [4b round]     按科目当前阶段判 round（见 §2.1 三阶段表）：
                      - 阶段 0（无锚点）：LLM 自由推理判
                      - 阶段 1（有锚点+rubric）：LLM 必须读 anchors_*.json + rubric_*.md 判
                      - 阶段 2（skill/脚本成熟）：强制脚本（如数学 d2_math_review_v4.py）
  └→ [5 validate]   py 脚本校验 chapter/KP/题型一致性 + KP 不在清单的题入等候区
  └→ [6 emit]       batch JSON 双写 assets/data/batches/ + question_bank/
  └→ [7 register]   question_bank/index.json + main.dart _bundledBatchAssets
  └→ [8 commit]     git commit + push + 更新 manifest
```

每步详细见 §10 实施细则。

### 2.1 Round 判定三阶段（V3.12.5 起硬规则）

每科 round 判定按"渐进沉淀"演化，**绝不退化**（每次 Famin verify 反馈后必须立即沉淀，让下批扫描受益）。

#### 三阶段定义

| 阶段 | 触发条件 | round 判定方式 | Famin 角色 |
|---|---|---|---|
| **0 无锚点** | 该科目首次扫描 / 锚点尚未建立 | LLM 自由推理判 | 抽审 verify markdown（锚点候选 + 改档）|
| **1 锚点+rubric** | 首轮 verify 反馈完成 → 锚点 + rubric 文档已沉淀 | **LLM 必须读 `anchors_*.json` + `rubric_*.md` 判**（不许凭空判，必须引用锚点 ID）| 继续抽审，反馈累积 |
| **2 skill/脚本** | rubric 稳定 + 累计 ≥ 3-4 轮 verify + 75% 严格对齐 | 脚本强制（python tools/d2_*_review_v*.py）| 仅 flag_review（high_variance / anchor_disagree）抽审 |

#### 各科目当前阶段（2026-05-08）

| 科目 | 阶段 | 判定方式 | 文档资产 |
|---|---|---|---|
| **数学** | ✅ 阶段 2 | `tools/d2_math_review_v4.py` 强制 | `~/.claude/skills/difficulty-math/` (anchors+evaluate+rubric+scripts) |
| **语文** | ⏳ 阶段 0→1（首轮 verify 进行中）| 等 Famin 审完 → 升阶段 1 | `docs/anchor_questions_g6_chinese.json`（20 道未审）+ rubric 待 Famin 审完后沉淀 |
| **英语** | ✅ 阶段 2（特殊路径）| Cambridge composition skill 自带 round（A2/B1/B2/C1 → R1/R2/R3/R4）| `~/.claude/skills/composition-english/`（待建）|
| **物理** | ⏳ 阶段 0 | LLM 自由判 | 待首轮扫描后建立锚点 |
| **化学** | ⏳ 阶段 0 | LLM 自由判 | 待首轮扫描后建立锚点 |

#### 阶段升级触发流程（强制）

每次 Famin verify 反馈后**立即**执行：

```
verify markdown 收到反馈
  ↓
1. 锚点更新：把同意的锚点写入 docs/anchor_questions_<grade>_<subject>.json
              + _famin_review.comment 字段
  ↓
2. Rubric 沉淀：把 reasoning 抽提到 docs/rubric_<subject>.md（首次→新建）
              重点记录"什么档对应什么特征" + "Famin 推翻 LLM 的典型 case"
  ↓
3. 反向修订：revert 项 → 改 batch round 字段（双写）+ commit
  ↓
4. 阶段判定：
     - 首轮 verify 完成 → 阶段 0 → 阶段 1（下批扫描 prompt 必须引用锚点+rubric）
     - 累计 ≥ 3-4 轮 verify + 75% 严格对齐 → 阶段 1 → 阶段 2（生成 reviewer 脚本 + skill）
  ↓
5. 工作流文件同步：
     - .realpaper-spec.md §2.1 各科阶段表更新
     - feedback_round_judge.md 同步
     - 新建 skill 时把锚点+rubric+脚本打包到 ~/.claude/skills/difficulty-<subject>/
```

**审了不沉淀 = 白审**。这是 feedback_strategy_to_execution 的硬要求。

#### 禁止行为

- ❌ 入库 agent 在 4a 阶段直接给 round 字段赋值（哪怕"为方便 4b 跑"）
- ❌ 数学科 LLM 自由判 round（已阶段 2，必须跑脚本）
- ❌ 已进入阶段 1 的科目，agent prompt 不引用锚点+rubric 凭空 LLM 判（这是退化）
- ❌ 跳过 verify 直接把 LLM 判的 round 落库（V3.12.5 commit 161072d 反面教材）
- ❌ Famin verify 完不沉淀就启动下批扫描（必须先沉淀再扫）

---

## 3. chapter 命名规则

### 3.1 数学/物理/化学：教材章节（保持现状）
严格映射 `lib/database/curriculum_seed.dart` 现有数学 40 / 物理 22 / 化学 11 chapter（按北师大/人教教材单元编排）。

### 3.2 语文：按 KP 一级 category
chapter = KP fullPath 的第一段（13 类）：`古诗文` / `现代文阅读` / `写作` / `字词` / `文学常识` / `名著阅读` / `修辞` / `综合性学习` / `句式与标点` / `语法` / `语法与句式` / `课文与名著` / `阅读理解`。

### 3.3 综合卷（小升初/中考）

| chapter | 适用 |
|---|---|
| `小升初综合` | grade=6 数学/语文 真题（覆盖小学全程内容）|
| `中考综合` | grade=9 数学/语文/物理/化学 真题（覆盖初一到初三）|

V3.10 已扩 6 个综合 chapter（curriculum_seed.dart）。

### 3.4 一卷归类策略

- **小升初真题** → grade=6, chapter="小升初综合", KP=题主考内容（可跨小学全年级）
- **中考真题** → grade=9, chapter="中考综合", KP=题主考内容（可跨初一到初三）
- **单元/月考/期末真题** → grade=对应年级, chapter=对应年级 curriculum_seed 章节, KP=题主考内容

文件名信息（地区/年份/类型）写到 `source` 字段，**不进 chapter**。

---

## 4. source 命名规则（每卷唯一）

```
realpaper_g{6-9}_<subject_pinyin>_<region_pinyin>_<paper_type>_<seq>
```

例：
- `realpaper_g6_math_beijing_pinggu_qimo_001`
- `realpaper_g9_chem_2018_zhongkao_shanghai_001`
- `realpaper_g6_chinese_xiaoshengchu_moni_school1_001`

`<seq>` 防同地区同类型多卷冲突。

### 4.1 主观题保留区
常规 source 基础上加 `_subj_held` 后缀：
- 例：`realpaper_g6_chinese_beijing_qimo_001_subj_held`
- QuestionDao 抽题 SQL 已加 `source NOT LIKE '%_subj_held'` 过滤
- 等接入 AI 评分 API 后批量去后缀启用

### 4.2 翻译为英文的中文题（V3.11+ 启用）
原中文题 source 加 `_translated_en` 后缀（暂不抽到，避免重复练）。

---

## 5. 题型识别规则

| 模式 | 判定 type |
|---|---|
| 题面含 4 行 `A./B./C./D.` | choice |
| 题面是单一陈述句 + `(    )` 或 `判断对错`（"对的画√错的画×"等指引）| **judgment**（V3.10 新）|
| 题面含 `_____` 或 `（  ）`且无 ABCD 选项；答案 ≤ 8 字 | fill |
| 题面含 `计算下列` `解方程` `列式` `求...的值` | calculation |
| 题面含 `作文` `根据材料写` `谈谈你的看法` `写一篇` `不少于 X 字` `谈谈感受` | **subjective**（保留区，source 加 `_subj_held`）|

### 5.1 输入法限制白名单（feedback_question_quality.md 继承）
fill 答案必须满足：
- 中文：单字 / ≤4 字短词 / ≤5 字古诗原文 / 修辞名词 / 作家作品名
- 数字：纯数字 / 简单分数（"1/3"）/ 简单比例（"3:1"）
- 不允许：长句 / 整段表达 / 含特殊符号（π/²/×/拼音声调等 → 改 choice）

### 5.2 主观题归类
- 语文作文/简答/论述 → subjective + `_subj_held`（家长批改机制 V3.8.3 已建，但暂不启用，等 AI API）
- 政治/历史的简答（不在范围）→ 跳过整题
- 数学/物理/化学的简答（如"简述实验步骤"）→ subjective + `_subj_held`

### 5.3 判断题（V3.10 新）
- type=`judgment`；answer 必须是 `"对"` 或 `"错"`（单字）
- AnswerMatcher 接受等价写法：对/正确/√/T/true/yes 视为"对"；错/错误/×/F/false/no 视为"错"
- options 字段填 `null`（UI 自动渲染对/错二选一按钮）
- alt_answers 不需要（matcher 已内置归一化）

### 5.4 系列组合题纪律（V3.12.5 全局原则，适用所有科目）

**总原则（绝对禁令）：**

> **绝不把一道大题拆成多道完全独立的题。** 必须保留共同题面，用 `group_id` 关联形成系列组合题。

适用所有科目（数学/语文/英语/物理/化学），以及所有有"共同上文"的复合结构：
- 大题指令 + 多个并列子问题（如"在下列句子中填关联词：(1)...(2)...(3)..."）
- 候选池 + 多个语境填空（如"从下列成语中选填：成语 A/B/C... (1)...(2)..."）
- 阅读材料 + 多个理解小题（如"阅读下文后回答：(1)...(2)...(3)..."）
- 图/表/材料 + 多个分析小题
- 数学应用题"已知条件 + 多问"（如"(1) 求...; (2) 在 (1) 的基础上...; (3) 拓展..."）
- 英语 cloze test、reading comprehension 多 question
- 物理化学的实验题、综合题

**统一字段约定：**

| 字段 | 含义 |
|---|---|
| `group_id` | 同一大题所有子题共享同一字符串（如 `"d1_kp1_q4"`）|
| `group_order` | 子题在大题内的顺序（1, 2, 3, ...）|
| `content` | **必须包含共同题面**（即使在多个子题里重复出现也保留），后接当前子题 |
| `type` | 子题类型独立判（同一组合题可同时含 fill / choice / judgment）|

**禁止做法：**

- ❌ 把子题切成完全独立的 question，丢弃共同题面（V3.12.5 d1_kp1 #13-16 / #17-20 反面教材：候选池/上文丢失）
- ❌ 用不同 group_id 把同一大题的子题分组（如 _a / _b 分子组）—— 同一大题必须同一 group_id
- ❌ 把整道大题压成一个 question（多空答案 AnswerMatcher 判不准 + UI 不友好）

**正确做法（举例）：**

部编 d1 单元卷 1 第 5 大题"在下列句子中填入恰当的关联词语：(1)过新年... (2)这___粽子，___小型的农业展览会... (3)今日腊八粥... (4)春节___很快乐..."

```yaml
共同题面: "在下列句子中填入恰当的关联词语。"
4 道子题，同一 group_id="d1_kp1_q5"，group_order=1..4：

子题 1 (group_order=1):
  type: fill
  content: "在下列句子中填入恰当的关联词语：过新年，小孩子(  )可以买到各种各样的玩意儿，(  )可以放鞭炮。请按顺序填两个关联词，用逗号分隔。"
  answer: "不但,而且"

子题 2 (group_order=2):
  type: fill
  content: "在下列句子中填入恰当的关联词语：这(  )粽子，(  )小型的农业展览会。请按顺序填两个关联词，用逗号分隔。"
  answer: "不是,而是"

# ... 子题 3 / 子题 4 同理
```

content 共同题面（"在下列句子中填入恰当的关联词语"）每道都重复一次——这是必须的，让题独立可读。

**UI 渲染（V3.8.2 已实现）：** 同 group_id 的题在练习/测评中按 group_order 连续呈现，错题集按"大题"聚合复盘。

**抽题策略：** 同 group_id 题群整体抽（不打散），保持原题的"系列做"体验。

**实施位置：** Pipeline §2 第 2 步 `segment.py` 必须识别"大题→子题"层级，切题时维持 group_id + group_order；4a annotate 阶段每道子题 content 必须含共同题面。无法判定大题边界时整道入"等候区"等人工审核。

### 5.5 复合题：fill + choice 类型混合（§5.4 的子情况）

**适用结构：** 大题先给一组待补全的词/句（A 部分），再用补全后的词去填语境空（B 部分）。典型如部编六下 d1 单元卷 1 第 4 大题：

```
4. 把下列四字词语补充完整，并选词填空。
   万(  )(  )新   (  )(  )不同   万不(  )(  )
   (  )灯(  )彩  (  )(  )分明   两(  )三(  )
   (1) 春节到了，中华大地上到处____，显出一派____的气象。
   (2) 在外边做事的人，除非____，必定赶回家来吃团圆饭。
   (3) 少数民族和汉族的饮食风俗虽然____，但各族人民热爱祖国、热爱民族大团结的心是相同的。
```

**遵循 §5.4 全局原则：所有子题用同一个 `group_id`（不分 _a / _b）**，type 可不同：
- A 部分子题 type=`fill`（补全字形）
- B 部分子题 type=`choice`（候选池 = A 部分补全后的词）

```yaml
group_id: "d1_kp1_q4"  # 整大题共享

A 部分（补全题，group_order=1..6）:
  type: fill
  content: "下列大题第 4 题为补全词语并选词填空。先补全 6 个四字词语，再用补全后的词回答 (1)(2)(3) 三句。当前小题：补全四字词语「万(  )(  )新」"
  answer: "象更"

B 部分（选词填空，group_order=7..9）:
  type: choice
  content: "下列大题第 4 题为补全词语并选词填空。补全后的 6 个成语：万象更新 / 截然不同 / 万不得已 / 张灯结彩 / 黑白分明 / 两面三刀。当前小题：(1) 春节到了，中华大地上到处(    )，显出一派___的气象。本题选第 1 个空。"
  options: ["万象更新", "截然不同", "万不得已", "张灯结彩", "黑白分明", "两面三刀"]
  answer: "张灯结彩"
```

**关键纪律：**
1. **同一 group_id**（不要分 _a / _b 子组，§5.4 已明确）
2. **每个子题 content 必须包含共同题面**（"下列大题第 4 题为补全词语并选词填空..."）
3. A 部分考字面识记 → fill
4. B 部分考候选辨析 → choice + options 显式列出 A 部分答案集

**多语境空的处理：**
- 一句含 N 个空（如"到处___，显出一派___"）→ 拆 N 道 choice，content 标"本题选第 K 个空"
- 候选池可重复使用 → options 列全部 6 个；候选池一次性消耗 → content 注明"每个成语只用一次"

**小变体：**
- A 部分极少（≤2 道）→ 仍拆 type，不要合并
- A 部分 = 课内默写（如"补全古诗：少壮不努力，___"）+ B 部分语境填 → A 仍 fill；B 候选池 = A 答案集

**判定 round：**
- A 部分 fill：字面识记，多数 R1
- B 部分 choice：候选池辨析，一般 R2；候选数 ≤ 3 且语境直白可降 R1

**实施位置：** Pipeline §2 第 2 步 `segment.py` 识别"...补充完整，并选词填空"或"...选择恰当的成语填入下列各句"等大题指令，按本节拆。无法判定时整道入"等候区"等人工审核。

---

## 6. 难度档（round）判定

由 Claude 主观判，基于 3 个维度：

### 6.1 试卷类型基线

| 试卷类型 | 整卷 round 基线 |
|---|---|
| 单元/小测 | R1 |
| 月考 | R1-R2 |
| 期中/期末 | R2-R3 |
| 中考真题 | R2-R4 全 |
| 培优/竞赛 | R3-R4 |

### 6.2 卷内位置加权
- 卷首选择前 1/3：基线 -1（最低 R1）
- 卷面中段：基线
- 卷尾大题/压轴：基线 +1（最高 R4）

### 6.3 KP 复杂度
- 单 KP 题：保持上述判定
- 跨年级综合题：**取最高年级 KP**（强制规则）
  - 7 + 9 年级 KP → 归 9 年级 KP
  - 7 + 8 + 9 → 归 9 年级 KP

### 6.4 round 含义（V3.8 沿用）
- R1：基础（教材内单一 KP 直接套用）
- R2：中等（同章节多 KP 综合）
- R3：较难（跨章节综合 / 应用题情境）
- R4：竞赛/压轴（跨年级综合 / 创新探究 / 拓展深度）

---

## 7. KP 严格匹配 + 等候区机制

### 7.1 严格匹配（feedback_kp_discipline.md 继承）
题的 `knowledge_point` 字段必须**严格**等于 `lib/database/knowledge_points_seed.dart` 的 fullPath（区分大小写、空格、括号）。

**禁止**自创变种 / 自动 add 新 KP。

### 7.2 等候区机制（V3.10 新）
不在 491 KP 清单的题：
- **不入库**
- 写到 `docs/realpaper_kp_pending.json`
- 含字段：source_file / batch_source / question_data（完整题对象 + KP=null）/ claude_suggestion (`candidate_kp` + `action: new_kp/merge_to/drop` + `merge_target` + `reason`)

### 7.3 每科 10 卷 review
我累计该科 10 卷的 `kp_pending` → 按候选 KP 聚类 → 给 Famin 看：
- 候选 KP 名
- 涉及题数
- 示例题面 2-3 个
- 我的建议（merge_to / new_kp / drop）
- 现有相近 KP（如有）

Famin 三选一：
1. **合并** → 题改 KP 后入 `realpaper_g{N}_{科}_kp_supplement` batch
2. **新建** → 先扩 `knowledge_points_seed.dart`，再入 supplement batch
3. **放弃** → 删等候区记录

---

## 8. 答案匹配

按优先级：

1. **同文件含答案段**（~52% 文件名带"含答案"/"试题及答案"）
   - 在 raw.txt 里找 `参考答案|答案|Answer Key` 之后段
   - 题号映射（"1." → 答案 "1." 后内容）

2. **独立答案文件**（同目录下 `*答案*` `*解析*` 同名文件）
   - 配对加载 + 题号对齐

3. **完全无答案**
   - Claude 会话级求解
   - 标记 `answer_source: claude_solved` 写入 explanation 末尾

---

## 9. 图片/质量纪律（Famin 决策）

### 9.1 完整性优先（V3.12.5 强化）：要么完整入库，要么整题放弃

**原则：** 一道题入库时**必须包含原题中的所有信息**——题面、候选池、共同上文、阅读材料、附图、附表、附说明等一个不漏。**任何一项识别不清或残缺，整题跳过**，不要"部分入库 + 缺什么就丢什么"。

**触发整题跳过的条件（任一满足即放弃）：**
- OCR 失败 / 扫描质量差 / 关键文字模糊
- 嵌入私有字体导致 pdftotext 出乱码
- 附图/几何图模糊看不清
- 听力音频质量差
- 阅读题原文缺失或被切断
- 候选池/共同上文残缺（参见 §5.4 系列组合题纪律）
- 任何"靠猜"才能补上的信息

**禁止做法：**
- ❌ "这道题文字看清了，图模糊就丢图入库" → 整题跳过
- ❌ "候选池没扫到，按语境硬填一个答案入库" → 整题跳过
- ❌ "答案配对模糊，但题目内容还能用，先入库再说" → 整题跳过
- ❌ "OCR 漏几个字，多模态/重绘补上" → 整题跳过
- ❌ "标 _unverified 等以后修" → 直接删除/不入库

**记录：** 跳过的题/卷写到 `manifest.skipped[]`，注明原因。**不要**浪费 token 抢救。

**理由：** 真题量足够（150+ 卷），找模糊题修复成本远高于换下一卷。残缺入库会污染题库（Famin V3.10 多模态幻觉伪题 122 道事件已是反面教材）。

详见 `feedback_unclear_abandon.md`（同一原则的记忆侧）。

### 9.2 清晰图片 → 直接截图嵌入
- 用 pdftoppm/pdf2image 截图 → base64 → image_data 字段（V3.7.6 schema 支持）
- 限制：base64 图 ≤ 200KB（避免 batch JSON 爆炸）

### 9.3 需要重绘的图 → 重绘
- 数学几何图 → SVG（V3.7.9 渲染支持）
- 物理电路图 → SVG
- 化学分子结构 → SVG 或 LaTeX
- 化学方程式 → LaTeX `$...$`（V3.7.9 已支持）
- 数学公式 → LaTeX `$...$`

### 9.4 重绘成本评估
- 简单图（直角坐标、几何方位）→ Claude 直接生成 SVG
- 复杂图（电路连线、立体几何）→ 评估时间 / 复杂度 → 不值得就跳过

---

## 10. 实施细则

### Step 1: extract.py
```python
# 用 libreoffice headless 转纯文本，sha1 缓存
sha1 = hashlib.sha1(open(path, 'rb').read()).hexdigest()
cache_dir = f'.cache/realpaper/{sha1}'
if not os.path.exists(f'{cache_dir}/raw.txt'):
    subprocess.run(['libreoffice', '--headless', '--convert-to', 'txt', '--outdir', cache_dir, path])
return open(f'{cache_dir}/raw.txt', encoding='utf-8').read()
```

### Step 2: segment.py
正则切题：
```
re.split(r'(?:^|\n)\s*(?:[一二三四五六七八九十]+[、.])', text)  # 大题
re.split(r'(?:^|\n)\s*\d+\s*[.、]', text)                    # 小题
re.split(r'(?:^|\n)\s*\(\d+\)', text)                        # 子题
```

### Step 3: match_ans.py
```python
# 找答案段
ans_start = re.search(r'(参考答案|答案|Answer Key|【答案】)', text)
if ans_start:
    ans_text = text[ans_start.end():]
    # 题号映射：1. xxx 2. xxx
    answers = dict(re.findall(r'(\d+)[.\s]+([^\n]+)', ans_text))
```

### Step 4: annotate（Claude inference）
对每题，Claude 输出 JSON：
```json
{
  "stem_idx": 5,
  "type": "choice",
  "chapter": "圆柱与圆锥",
  "knowledge_point": "圆柱与圆锥/圆柱体积",
  "round": 2,
  "explanation": "..."
}
```

### Step 5: validate.py
检查：
- chapter ∈ curriculum_seed（按 subject + grade 过滤）
- knowledge_point ∈ knowledge_points_seed（按 subject 过滤）
- type 一致（题面有 ABCD ↔ type=choice）
- 不在清单的 KP → 转 kp_pending.json

### Step 6: emit batch JSON
```json
{
  "source": "realpaper_g6_math_beijing_pinggu_qimo_001",
  "subject": "math",
  "grade": 6,
  "knowledge_points_added": [],
  "questions": [...]
}
```

双写到：
- `assets/data/batches/<source>.json`
- `question_bank/<source>.json`

### Step 7: register
- `question_bank/index.json` 加 batches[] 项 + version+1
- `lib/main.dart` `_bundledBatchAssets` 追加 `'assets/data/batches/<source>.json'`

### Step 8: commit
- `git add` 上述所有改动
- `git commit -m "Realpaper batch: <region> <grade> <subject> <type>"`
- `git push origin main`
- 更新 `docs/realpaper_manifest.json`

---

## 11. observations 反哺（各科独立）

每处理 5 卷，追加对应 observations 文档：
- `docs/realpaper_observations_math.md`
- `docs/realpaper_observations_chinese.md`
- `docs/realpaper_observations_physics.md`
- `docs/realpaper_observations_chemistry.md`

记录：
- 题面句型套路
- 干扰项设计模式
- 难度梯度规律
- KP 组合习惯
- 答案颗粒度
- 卷型差异

下次会话开始时**必读**对应 observations，提升标注效率与质量。

---

## 12. 失败回滚 / 错误处理

| 错误类型 | 处理 |
|---|---|
| libreoffice 转换失败 | manifest.skipped + 等待手工处理 |
| OCR 扫描质量太差 | 整文件跳过 + manifest.skipped |
| 切题失败（题号识别不出）| 整文件跳过 + manifest.issues |
| 答案缺失 | Claude 求解 + explanation 标 `answer_source: claude_solved` |
| KP 不在清单 | 入 kp_pending.json |
| chapter 不在清单 | 这是 bug，必须修（不允许进等候区）|
| 题型识别歧义 | Claude 二选一 + explanation 注明 |
| 同 sha1 文件已处理 | 跳过整文件（manifest 校验）|
| 单 commit 题量过大（> 500）| 拆批 commit |

---

## 13. 不要做的事

- ❌ 不要改老 cron AI 出的题包（12 个 batch 已 deprecated）
- ❌ 不要使用清单外的 chapter（必须严格映射 curriculum_seed）
- ❌ 不要使用清单外的 KP（不在清单 → 入等候区）
- ❌ 不要自动 add 新 KP（必须 Famin 拍板）
- ❌ 不要把英语真题入库（英语全走 Cambridge AI）
- ❌ 不要为不清晰的图浪费 token 修复
- ❌ 不要把主观题（作文/简答）入正式抽题池（必须 `_subj_held` 后缀）
- ❌ 不要 push 含敏感信息（PAT / API key）

---

**版本：** 2026-05-08 V1（V3.10 立项时由 Famin 与 Claude 共同制定）
